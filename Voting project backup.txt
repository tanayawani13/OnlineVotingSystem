.........src->main->java->controller->




.........ActorVoteServlet.java

package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;

@WebServlet("/ActorVoteServlet")
public class ActorVoteServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        // get selected actor
        int actor = Integer.parseInt(req.getParameter("actor"));

        // store in session
        HttpSession session = req.getSession();
        session.setAttribute("actor", actor);

        // redirect to next page
        res.sendRedirect("actress.jsp");
    }
}








.............ActressVoteServlet.java



package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;

@WebServlet("/ActressVoteServlet")
public class ActressVoteServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        // get selected actress
        int actress = Integer.parseInt(req.getParameter("actress"));

        // store in session
        HttpSession session = req.getSession();
        session.setAttribute("actress", actress);

        // redirect to final page
        res.sendRedirect("villain.jsp");
    }
}
















.......AddCategoryServlet2.java

package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/AddCategoryServlet")
public class AddCategoryServlet2 extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        String category = req.getParameter("category");

        try {
            Connection con = DBConnection.getConnection();

            PreparedStatement ps = con.prepareStatement(
                "INSERT INTO categories(name) VALUES (?)"
            );

            ps.setString(1, category);
            ps.executeUpdate();

            res.getWriter().println("Category Added Successfully!");

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}





..........AddNomineeServlet.java


package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/AddNomineeServlet")
public class AddNomineeServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        int categoryId = Integer.parseInt(req.getParameter("categoryId"));
        String nominee = req.getParameter("nominee");
        
        String image = req.getParameter("image");

        try {
            Connection con = DBConnection.getConnection();

            PreparedStatement ps = con.prepareStatement(
            		 "INSERT INTO nominees(category_id, nominee_name, image_path) VALUES (?, ?, ?)"
            );

            ps.setInt(1, categoryId);
            ps.setString(2, nominee);
            ps.setString(3, image);

            ps.executeUpdate();

            res.sendRedirect("adminDashboard.jsp?msg=added");
            
        }catch (Exception e) {
            e.printStackTrace();
            res.getWriter().println("ERROR: " + e.getMessage());
        }
    }
}








..........AdminLoginServlet.java

package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/AdminLoginServlet")
public class AdminLoginServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        String username = req.getParameter("username");
        String password = req.getParameter("password");

        try {
            Connection con = DBConnection.getConnection();

            PreparedStatement ps = con.prepareStatement(
                "SELECT * FROM admin WHERE username=? AND password=?"
            );

            ps.setString(1, username);
            ps.setString(2, password);

            ResultSet rs = ps.executeQuery();

            if (rs.next()) {
                HttpSession session = req.getSession();
                session.setAttribute("admin", username);

                res.sendRedirect("adminDashboard.jsp");
            } else {
                res.getWriter().println("Invalid Admin Login!");
            }

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}





............FinalVoteServlet.java




package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/FinalVoteServlet")
public class FinalVoteServlet extends HttpServlet {

	protected void doPost(HttpServletRequest req, HttpServletResponse res)
	        throws ServletException, IOException {

	    res.setContentType("text/html");

	    try {
	        HttpSession session = req.getSession();

	        Integer userId = (Integer) session.getAttribute("userId");
	        Integer movie = (Integer) session.getAttribute("movie");
	        Integer actor = (Integer) session.getAttribute("actor");
	        Integer actress = (Integer) session.getAttribute("actress");

	        int villain = Integer.parseInt(req.getParameter("villain"));

	        // 🔴 DEBUG CHECK (VERY IMPORTANT)
	        if (userId == null || movie == null || actor == null || actress == null) {
	            res.getWriter().println("Session expired! Please login again.");
	            return;
	        }

	        Connection con = DBConnection.getConnection();
	        
	        PreparedStatement check = con.prepareStatement(
	        	    "SELECT * FROM votes WHERE user_id=?"
	        	);
	        	check.setInt(1, userId);
	        	ResultSet rs = check.executeQuery();

	        	if (rs.next()) {
	        	    res.sendRedirect("home.jsp?msg=already");
	        	    return;
	        	}

	        insertVote(con, userId, 1, movie);
	        insertVote(con, userId, 2, actor);
	        insertVote(con, userId, 3, actress);
	        insertVote(con, userId, 4, villain);

	        // clear session
	        session.removeAttribute("movie");
	        session.removeAttribute("actor");
	        session.removeAttribute("actress");

	        // ✅ REDIRECT (NO BLANK PAGE)
	        res.sendRedirect("home.jsp?msg=success");

	    } catch (Exception e) {
	        e.printStackTrace();

	        // 🔴 SHOW ERROR ON SCREEN (VERY IMPORTANT FOR DEBUG)
	        res.getWriter().println("ERROR: " + e.getMessage());
	    }
	}

    private void insertVote(Connection con, int userId, int categoryId, int nomineeId) throws Exception {

        PreparedStatement ps = con.prepareStatement(
            "INSERT INTO votes(user_id, category_id, nominee_id) VALUES (?, ?, ?)"
        );
        ps.setInt(1, userId);
        ps.setInt(2, categoryId);
        ps.setInt(3, nomineeId);
        ps.executeUpdate();

        PreparedStatement ps2 = con.prepareStatement(
            "UPDATE nominees SET vote_count = vote_count + 1 WHERE id=?"
        );
        ps2.setInt(1, nomineeId);
        ps2.executeUpdate();
    }
}











.............LoginServlet.java


package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/LoginServlet")
public class LoginServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        String email = req.getParameter("email");
        String password = req.getParameter("password");

        try {
            Connection con = DBConnection.getConnection();

            PreparedStatement ps = con.prepareStatement(
                "SELECT * FROM users WHERE email=? AND password=?"
            );

            ps.setString(1, email);
            ps.setString(2, password);

            ResultSet rs = ps.executeQuery();

            if (rs.next()) {
                HttpSession session = req.getSession();
                session.setAttribute("userId", rs.getInt("id"));
                session.setAttribute("userName", rs.getString("fullname"));

                // ✅ Redirect to voting page
                res.sendRedirect("awards.jsp");

            } else {
                res.getWriter().println("Invalid Email or Password!");
            }

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}




.............LogoutServlet.java


package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;

@WebServlet("/LogoutServlet")
public class LogoutServlet extends HttpServlet {

    protected void doGet(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        // Destroy session
        HttpSession session = req.getSession(false);

        if (session != null) {
            session.invalidate();
        }

        // Redirect to home page
        res.sendRedirect("home.jsp");
    }
}











..............MovieVoteServlet.java




package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;

@WebServlet("/MovieVoteServlet")
public class MovieVoteServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        // get selected movie
        int movie = Integer.parseInt(req.getParameter("movie"));

        // store in session
        HttpSession session = req.getSession();
        session.setAttribute("movie", movie);

        // redirect to next step
        res.sendRedirect("actor.jsp");
    }
}












................RegisterServlet.java


package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;
import java.util.*;

import dao.DBConnection;

@WebServlet("/RegisterServlet")
public class RegisterServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {
    	
    	res.setContentType("text/html");
    	System.out.println("RegisterServlet started");

    	String fullname = req.getParameter("fullname");
    	String email = req.getParameter("email");
    	String password = req.getParameter("password");
    	String confirmPassword = req.getParameter("confirmPassword");

    	// ✅ EMAIL VALIDATION
    	String emailRegex = "^[A-Za-z0-9+_.-]+@(.+)$";
    	if (!email.matches(emailRegex)) {
    	    res.getWriter().println("Invalid Email Format!");
    	    return;
    	}

    	// ✅ PASSWORD VALIDATION
    	String passwordRegex = "^(?=.*[A-Z])(?=.*[a-z])(?=.*[0-9])(?=.*[@#$%^&+=]).{6,}$";
    	if (!password.matches(passwordRegex)) {
    	    res.getWriter().println("Password must contain uppercase, lowercase, number and special symbol!");
    	    return;
    	}
        
        try {
            Connection con = DBConnection.getConnection();
//            PrintWriter pw=new PrintWriter();
//            pw.println("Helloo......!");
            // ✅ Password Match Check
            if (!password.equals(confirmPassword)) {
                res.getWriter().println("Password does not match!");
                return;
            }

            // ✅ Check Duplicate Email
            PreparedStatement check = con.prepareStatement(
                    "SELECT * FROM users WHERE email=?");
            check.setString(1, email);
            ResultSet rs = check.executeQuery();

            if (rs.next()) {
                res.getWriter().println("Email already exists!");
                return;
            }

            
            
            // ✅ INSERT USER (MAIN QUERY)
            PreparedStatement ps = con.prepareStatement(
                    "INSERT INTO users(fullname, email, password) VALUES (?, ?, ?)");
            
            ps.setString(1, fullname);
            ps.setString(2, email);
            ps.setString(3, password);

            int i = ps.executeUpdate();

            if (i > 0) {
                res.sendRedirect("login.jsp");
            } else {
                res.getWriter().println("Registration Failed!");
            }

        }
        catch (Exception e) {
            e.printStackTrace();
            res.getWriter().println("ERROR: " + e.getMessage());
        }
    }
}       




...........VoteServlet.java


package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/VoteServlet")
public class VoteServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        HttpSession session = req.getSession();
        Integer userId = (Integer) session.getAttribute("userId");

        if (userId == null) {
            res.sendRedirect("login.jsp");
            return;
        }

        try {
            Connection con = DBConnection.getConnection();

            // ✅ Check if user already voted
            PreparedStatement check = con.prepareStatement(
                "SELECT * FROM votes WHERE user_id=?"
            );
            check.setInt(1, userId);
            ResultSet rs = check.executeQuery();

            if (rs.next()) {
                res.getWriter().println("You have already voted!");
                return;
            }

            // Get values
            int movie = Integer.parseInt(req.getParameter("movie"));
            int actor = Integer.parseInt(req.getParameter("actor"));
            int actress = Integer.parseInt(req.getParameter("actress"));
            int song = Integer.parseInt(req.getParameter("song"));

            // Insert votes
            insertVote(con, userId, 1, movie);
            insertVote(con, userId, 2, actor);
            insertVote(con, userId, 3, actress);
            insertVote(con, userId, 4, song);

            res.sendRedirect("result.jsp");

        } catch (Exception e) {
            e.printStackTrace();
        }
    }

    private void insertVote(Connection con, int userId, int categoryId, int nomineeId) throws Exception {

        PreparedStatement ps = con.prepareStatement(
            "INSERT INTO votes(user_id, category_id, nominee_id) VALUES (?, ?, ?)"
        );
        ps.setInt(1, userId);
        ps.setInt(2, categoryId);
        ps.setInt(3, nomineeId);
        ps.executeUpdate();

        PreparedStatement ps2 = con.prepareStatement(
            "UPDATE nominees SET vote_count = vote_count + 1 WHERE id=?"
        );
        ps2.setInt(1, nomineeId);
        ps2.executeUpdate();
    }
}




.........src->main->java->dao->


.......DBConnection.java


package dao;

import java.sql.Connection;
import java.sql.DriverManager;

public class DBConnection {

    public static Connection getConnection() {
        Connection con = null;

        try {
            // Load Driver
            Class.forName("org.postgresql.Driver");

            // DB details
            String url = "jdbc:postgresql://localhost:5432/online_voting_db";
            String user = "postgres";
            String password = "1234";   // 🔴 CHANGE THIS

            con = DriverManager.getConnection(url, user, password);

            System.out.println("Database Connected Successfully!");

        } catch (Exception e) {
            e.printStackTrace();
        }

        return con;
    }
}







........TestDB.java




package dao;

public class TestDB {
    public static void main(String[] args) {
        if(DBConnection.getConnection() != null){
            System.out.println("Connected to database successfully!");
        } else {
            System.out.println("Connection failed!");
        }
    }
}






.........src->mian-> webapp->css->



....style.css


/* RESET */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* BODY - GRADIENT + ANIMATION */
body {
    font-family: 'Poppins', 'Segoe UI Emoji', 'Noto Color Emoji', sans-serif;
    background: linear-gradient(135deg, #1a0026, #3a0ca3, #7209b7, #f72585);
    background-size: 400% 400%;
    animation: gradientBG 10s ease infinite;
    color: #fff;
    text-align: center;
}

/* BACKGROUND ANIMATION */
@keyframes gradientBG {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}

/* HEADINGS */
h1 {
    font-size: 42px;
    color: gold;
    text-shadow: 0 0 15px gold;
    margin-top: 20px;
}

h2 {
    margin: 10px;
    color: #ffccff;
}

h3 {
    color: #ffd6ff;
    margin-top: 20px;
}

/* CONTAINER - GLASS EFFECT */
.container {
    width: 65%;
    margin: 30px auto;
    padding: 30px;
    border-radius: 20px;
    background: rgba(255,255,255,0.1);
    backdrop-filter: blur(15px);
    box-shadow: 0 0 30px rgba(255,255,255,0.3);
}

/* INPUT (FORMS) */
input {
    width: 75%;
    padding: 10px;
    border-radius: 10px;
    border: none;
    margin-top: 0;        /* 🔥 remove extra gap */
}

/* FIX RADIO BUTTON SIZE */
input[type="radio"] {
    width: auto;
}

/* BUTTON */
button {
    background: linear-gradient(45deg, #ffbe0b, #fb5607);
    border: none;
    padding: 10px 20px;
    border-radius: 12px;
    margin: 10px;
    color: white;
    font-weight: bold;
    cursor: pointer;
    box-shadow: 0 0 10px #ffbe0b;
    transition: 0.3s;
}

/* BUTTON HOVER */
button:hover {
    transform: scale(1.1);
    background: linear-gradient(45deg, #ff006e, #8338ec);
}

/* CENTER ONLY SUBMIT BUTTON */
.btn-center {
    text-align: center;
}

.btn-center button {
    margin: 20px auto;
}

/* LINKS */
a {
    text-decoration: none;
}

/* LABEL (for voting page alignment) */
label {
    display: block;
    margin-top: 10px;
    margin-bottom: 2px;   /* 🔥 very small gap */
    text-align: center;
}


/* TABLE (RESULT / VOTES) */
table {
    margin: 30px auto;
    border-collapse: collapse;
    width: 60%;
    background: rgba(255,255,255,0.1);
    border-radius: 15px;
    overflow: hidden;
}

th, td {
    padding: 12px;
    border: 1px solid white;
}

th {
    background: #ff006e;
    color: white;
}

tr:hover {
    background: rgba(255,255,255,0.2);
}

/* FOOTER */
footer {
    margin-top: 40px;
    padding: 15px;
    background: rgba(0,0,0,0.3);
}

/* TAGLINE COLORS */
.tagline {
    font-size: 20px;
    margin-bottom: 20px;
}

.highlight1 { color: #ffbe0b; }
.highlight2 { color: #00f5d4; }
.highlight3 { color:#ffbe0b; }
.highlight4 { color: #00f5d4; }

/* CARD STYLE (ABOUT & RULES) */
.card {
    background: rgba(255,255,255,0.15);
    padding: 20px;
    margin: 20px 0;
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(255,255,255,0.2);
    text-align: left;
}

/* ABOUT & RULES TITLES */
.about-title {
    color: #00f5d4;
    text-shadow: 0 0 10px #00f5d4;
}

.rules-title {
    color: #ffbe0b;
    text-shadow: 0 0 10px #ffbe0b;
}

/* TROPHY ICON */
.trophy {
    font-size: 40px;
    margin: 0 10px;
}

/* TYPING TEXT (JS BASED) */
#typingText {
    font-size: 42px;
    color: gold;
    text-shadow: 0 0 15px gold;
}

/* OPTIONAL INPUT FOCUS EFFECT */
input:focus {
    outline: none;
    box-shadow: 0 0 10px #ffbe0b;
}	









.typing {
    display: inline-block;
    overflow: hidden;
    white-space: nowrap;
    border-right: none !important;

    font-size: 42px;
    color: gold;
    text-shadow: 0 0 15px gold;

    width: 0;
    animation: typing 4s steps(28, end) forwards;
}







.typing::after {
    content: none !important;
}





/* typing animation */
@keyframes typing {
    from { width: 0 }
    to { width: 28ch; }  /* adjust if needed */
}

/* cursor blink */
<!-- @keyframes blink {
    50% { border-color: transparent; }
}  -->





.vote-option {
    display: flex;
    align-items: center;
    gap: 15px;
    margin: 10px 0;
    justify-content: flex-start;
    margin-left: 20%;
}

.vote-option img {
    width: 60px;
    height: 70px;
    border-radius: 10px;
}










.category-box {
    background: rgba(255,255,255,0.12);
    padding: 20px;
    margin: 25px 0;
    border-radius: 15px;
    box-shadow: 0 0 20px rgba(255,255,255,0.2);
}















/*trohy animation
@keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
}









/* ===== WINNER CARD FINAL CLEAN ===== */

.winner-card {
    text-align: center;
    margin-bottom: 40px;
    padding: 25px;
    border-radius: 20px;
    background: rgba(255, 215, 0, 0.1);
    border: 2px solid gold;
    box-shadow: 0 0 25px gold;
    transition: all 0.4s ease;
}

/* 🔥 CARD HOVER */
.winner-card:hover {
    transform: scale(1.07);
    box-shadow: 0 0 50px gold;
}

/* 🏆 TITLE */
.winner-title {
    color: gold;
    font-size: 28px;
    text-shadow: 0 0 10px gold;
    margin-bottom: 10px;
}

/* 📸 FIXED IMAGE BOX (MAIN FIX) */
.image-box {
    width: 250px;
    height: 250px;
    margin: 0 auto;
    overflow: hidden;
    border-radius: 15px;
}

/* ✅ PERFECT IMAGE FIT */
.winner-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: 0.4s;
}

/* 🔥 IMAGE ZOOM */
.winner-card:hover .winner-img {
    transform: scale(1.1);
}

/* ✨ NAME */
.winner-name {
    margin-top: 12px;
    font-size: 24px;
    font-weight: bold;
    color: #00f5d4;
    text-shadow: 0 0 10px #00f5d4;
}











.........src->mian-> webapp->









............actor.jsp


<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<%@ page isErrorPage="true" %>
<%
    if (session.getAttribute("userId") == null) {
        response.sendRedirect("login.jsp");
        return;
    }
%>

<!DOCTYPE html>
<html>
<head>
    <title>Vote Now</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">

<h2 style="color:#00f5d4; font-size:40px; text-shadow:0 0 10px #00f5d4;">
    Choose Your Bollywood Favorites
</h2><br>

<form action="ActorVoteServlet" method="post">

<div class="category-box">

<h3 style="font-size:28px; color:#ffbe0b; text-shadow:0 0 10px #ffbe0b;">
    Step 2/4 - Best Actor
</h3><br><br>

<hr style="border:none; height:2px; background:linear-gradient(to right, transparent, white, transparent); margin:10px 0 20px;">

<%
try {
    Connection con = DBConnection.getConnection();

    // 🔥 category_id = 2 → Actor
    PreparedStatement ps = con.prepareStatement(
        "SELECT * FROM nominees WHERE category_id = 2"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<label class="vote-option" style="font-size:20px;">
    <input type="radio" name="actor" value="<%= rs.getInt("id") %>" required>
    
    <img src="<%= rs.getString("image_path") %>" 
         style="width:140px; height:120px;">
    
    <%= rs.getString("nominee_name") %>
</label>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</div>

<br><br>

<div class="btn-center">
    <button type="submit">Next</button>
</div>

</form>

</div>

</body>
</html>





.............actress.jsp



<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<%@ page isErrorPage="true" %>
<%
    if (session.getAttribute("userId") == null) {
        response.sendRedirect("login.jsp");
        return;
    }
%>

<!DOCTYPE html>
<html>
<head>
    <title>Vote Now</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">

<h2 style="color:#00f5d4; font-size:40px; text-shadow:0 0 10px #00f5d4;">
   Choose Your Bollywood Favorites
</h2><br>

<form action="ActressVoteServlet" method="post">

<div class="category-box">

<h3 style="font-size:28px; color:#ffbe0b; text-shadow:0 0 10px #ffbe0b;">
    Step 3/4 - Best Actress
</h3><br><br>

<hr style="border:none; height:2px; background:linear-gradient(to right, transparent, white, transparent); margin:10px 0 20px;">

<%
try {
    Connection con = DBConnection.getConnection();

    // 🔥 category_id = 3 → Actress
    PreparedStatement ps = con.prepareStatement(
        "SELECT * FROM nominees WHERE category_id = 3"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<label class="vote-option" style="font-size:20px;">
    <input type="radio" name="actress" value="<%= rs.getInt("id") %>" required>

    <img src="<%= rs.getString("image_path") %>" 
         style="width:140px; height:120px;">

    <%= rs.getString("nominee_name") %>
</label>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</div>

<br><br>

<div class="btn-center">
    <button type="submit">Next</button>
</div>

</form>

</div>

</body>
</html>










.............addCategory.jsp


<div class="container">
<!DOCTYPE html>
<html>
<head>
    <title>Add Category</title>
	<link rel="stylesheet" href="css/style.css">
</head>
<body>

<div class="container">

<h2 style="color: cyan;">Add Category</h2><br>

<form action="AddCategoryServlet" method="post">

    <label style="font-size:20px;">Category Name</label><br>
    <input type="text" name="category" placeholder="Enter category name"  required><br><br>


    <!-- ✅ CENTER BUTTON -->
    <div class="btn-center">
        <button type="submit">Add</button>
    </div>

</form>

</div>


</body>
</html>
</div>


















....................addNominee.jsp







<div class="container">
<!DOCTYPE html>
<html>
<head>
    <title>Add Nominee</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<div class="container">

<h2 style="color: cyan;">Add Nominee</h2><br>

<form action="AddNomineeServlet" method="post">

    
    <label style="font-size:20px;">Select Category</label>
	<select name="categoryId" required 
        style="width:75%; padding:10px; border-radius:10px; border:none;">
        
    	<option value="1">Best Movie</option>
    	<option value="2">Best Actor</option>
    	<option value="3">Best Actress</option>
    	<option value="4">Best Villain</option>
	</select>
<br><br>

    <label style="font-size:20px;">Nominee Name</label>
    <input type="text" name="nominee" placeholder="Enter Nominee Name"  required><br><br>
    
    <label style="font-size:20px;">Image Path</label>
<input type="text" name="image" placeholder="images/xyz.jpg" required>
<br><br>

    <!-- ✅ CENTER BUTTON -->
    <div class="btn-center">
        <button type="submit">Add</button>
    </div>

</form>

</div>

</body>
</html>
</div>































.............adminDashboard.jsp

<%@ page isErrorPage="true" %>



<%
    String msg = request.getParameter("msg");

    if ("added".equals(msg)) {
%>
<script>
    alert("Nominee Added Successfully!");
</script>
<%
    }
%>


<%
    if (session.getAttribute("admin") == null) {
        response.sendRedirect("adminLogin.jsp");
        return;
    }
%>

<!DOCTYPE html>
<html>
<head>
    <title>Admin Dashboard</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">

    <!-- TITLE -->
    <h2 style="color:#00f5d4; font-size:40px; text-shadow:0 0 10px #00f5d4;">
        Admin Dashboard
    </h2>

    <br>

    <!-- DASHBOARD OPTIONS -->
    <div class="card" style="text-align:center;">

       

        <a href="addNominee.jsp">
            <button style="width:250px;"> Add Nominee</button>
        </a>
        <br><br>

        <a href="viewVotes.jsp">
            <button style="width:250px;"> View Votes</button>
        </a>
        <br><br>

        <a href="declareWinner.jsp">
            <button style="width:250px;"> Declare Winner</button>
        </a>

    </div>

    <br><br>

    <!-- LOGOUT BUTTON -->
    <div class="btn-center">
        <a href="LogoutServlet">
            <button> Logout</button>
        </a>
    </div>

</div>

</body>
</html>




.............adminLogin.jsp


<div class="container">
<!DOCTYPE html>
<html>
<head>
    <title>Admin Login</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<div class="container">

<h2 style="color: cyan;"> Admin Login </h2><br>

<form action="AdminLoginServlet" method="post">

    <label style="font-size:20px;">Username</label>
    <input type="text" name="username" placeholder="Enter username"  required><br><br>

    <label style="font-size:20px;">Password</label>
    <input type="password" name="password" placeholder="Enter Password"  required><br><br>

    <!-- ✅ CENTER BUTTON -->
    <div class="btn-center">
        <button type="submit">Login</button>
    </div>

</form>

</div>

</body>
</html>
</div>

































..................awards.jsp


<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<%@ page isErrorPage="true" %>
<%
    if (session.getAttribute("userId") == null) {
        response.sendRedirect("login.jsp");
        return;
    }
%>

<!DOCTYPE html>
<html>
<head>
    <title>Vote Now</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">

<h2 style="color:#00f5d4; font-size:40px; text-shadow:0 0 10px #00f5d4;">
    Choose Your Bollywood Favorites
</h2><br>

<form action="MovieVoteServlet" method="post">

<div class="category-box">

<h3 style="font-size:28px; color:#ffbe0b; text-shadow:0 0 10px #ffbe0b;">
    Step 1/4 - Best Movie
</h3><br><br>

<hr style="border:none; height:2px; background:linear-gradient(to right, transparent, white, transparent); margin:10px 0 20px;">

<%
try {
    Connection con = DBConnection.getConnection();

    // 🔥 category_id = 1 → Movie
    PreparedStatement ps = con.prepareStatement(
        "SELECT * FROM nominees WHERE category_id = 1"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<label class="vote-option" style="font-size:20px;">
    <input type="radio" name="movie" value="<%= rs.getInt("id") %>" required>

    <img src="<%= rs.getString("image_path") %>" 
         style="width:140px; height:120px;">

    <%= rs.getString("nominee_name") %>
</label>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</div>

<br><br>

<div class="btn-center">
    <button type="submit">Next</button>
</div>

</form>

</div>

</body>
</html>




....................declareWinner.jsp





<div class="container">
<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<!DOCTYPE html>
<html>
<head>
    <title>Winners</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<h2 style="color: cyan;" style="display:flex; align-items:center; justify-content:center; gap:10px;">
<img src="images/trophy.png" style="width:70px; filter: drop-shadow(0 0 10px gold);">
And the Winners are.....</h2>


<table border="1">
<tr>
    <th>Category</th>
    <th>Winner</th>
    <th>Votes</th>
</tr>

<%
try {
    Connection con = DBConnection.getConnection();

    PreparedStatement ps = con.prepareStatement(
        "SELECT c.name, n.nominee_name, n.vote_count " +
        "FROM nominees n " +
        "JOIN categories c ON n.category_id = c.id " +
        "WHERE n.vote_count = (" +
        "SELECT MAX(n2.vote_count) FROM nominees n2 WHERE n2.category_id = n.category_id)"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<tr>
    <td><%= rs.getString("name") %></td>
    <td><%= rs.getString("nominee_name") %></td>
    <td><%= rs.getInt("vote_count") %></td>
</tr>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</table>

<br>
<a href="home.jsp">Back to Home</a>

</body>
</html>
</div>







.......................home.jsp


<!DOCTYPE html>
<html>
<head>
    <title>Online Award Voting</title>
    <link rel="stylesheet" href="css/style.css">

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;600&display=swap" rel="stylesheet">
    <meta charset="UTF-8">
    <link rel="stylesheet" href="${pageContext.request.contextPath}/css/style.css">
    
    
    
</head>

<body>





<%
    String msg = request.getParameter("msg");

    if ("success".equals(msg)) {
%>
<script>
    alert("Your vote has been submitted successfully!");
</script>
<%
    } else if ("already".equals(msg)) {
%>
<script>
    alert("You have already voted! Duplicate voting is not allowed.");
</script>
<%
    }
%>



<!-- 🏆 TROPHY IMAGE -->
<img src="images/trophy.png" 
     alt="Trophy"
     style="width:120px; margin-top:10px; filter: drop-shadow(0 0 15px gold);">


<!-- 🔥 ANIMATED TITLE -->	
<h1 class="typing">Bollywood Hungama Awards 2026</h1>

<!--<h1 id="typingText"></h1> -->

<!-- 🏆 TROPHY IMAGE -->
<img src="images/trophy.png" 
     alt="Trophy"
     style="width:120px; margin-top:10px; filter: drop-shadow(0 0 15px gold);">



<h2>Online Voting System</h2>

<br>
<!-- <img src="images/home.jpg"
     style="width:40%; max-width:300px; transition:0.4s;"
     onmouseover="this.style.transform='scale(1.05)'"
     onmouseout="this.style.transform='scale(1)'">
     <br>   -->

<div class="container">

<div class="winner-card">  <!-- 🔥 ONE BIG GOLD BOX -->


<p class="tagline">
    Vote for your favorite 
    <span class="highlight1">movies</span>, 
    <span class="highlight2">actors</span>, 
    <span class="highlight3">actresses</span> & 
    <span class="highlight4">villains</span>
</p>


<!--  <img src="images/home.jpg"
     style="width:40%; max-width:300px; transition:0.4s;"
     onmouseover="this.style.transform='scale(1.05)'"
     onmouseout="this.style.transform='scale(1)'">      -->

<!-- ABOUT CARD -->
<div class="card">
    <h3 class="about-title">About</h3>
    <p>
        The Online Award Voting System is a web-based platform designed
        to allow users to vote for their favorite movies, actors, actresses,
        and songs in a fair and transparent manner.
        <br><br>
        Each registered user can participate and cast their vote securely.
        The system is simple, fast, and accessible to everyone.
    </p>
</div>



<!-- RULES CARD -->
<div class="card">
    <h3 class="about-title">Rules</h3>
    <ul class="rules-list">
        <li>Only registered users can vote.</li>
        <li>Each user can vote only once.</li>
        <li>No duplicate voting allowed.</li>
        <li>Results are public.</li>
    </ul>
</div>

</div> <!-- 🔥 END GOLD BOX -->

</div>

<br>

<!-- ACTION BUTTONS -->
<div class="actions">
    <a href="register.jsp"><button>Register</button></a>
    <a href="login.jsp"><button>Login</button></a>
    <a href="result.jsp"><button>View Results</button></a>
    <a href="viewVotes.jsp"><button>Vote Count</button></a>
    <a href="adminLogin.jsp"><button>Admin</button></a>
</div>

<footer>#Bollywood Hungama Awards 2026</footer>

</body>
</html>









....................login.jsp



<!DOCTYPE html>
<html>
<head>
    <title>User Login</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<div class="container">

<h2 style="color: cyan;">Login Here</h2><br>

<form action="LoginServlet" method="post">

    <label style="font-size:20px;">Email</label>
    <input type="email" name="email" placeholder="Enter Email as Username" required><br><br>

    <label style="font-size:20px;">Password</label>
    <input type="password" name="password" placeholder="Enter Password" required><br><br>

    <!-- ✅ CENTER BUTTON -->
    <div class="btn-center">
        <button type="submit">Login</button>
    </div>

</form>

</div>

</body>
</html>






.............register.jsp



<!DOCTYPE html>
<html>
<head>
    <title>User Registration</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<div class="container"> 

   <h2 style="color: cyan;">Register Here</h2><br>
    <form action="RegisterServlet" method="post">
   
    

        <label style="font-size:20px;">Full Name</label>
        <input type="text" name="fullname" placeholder="Enter your name" required><br><br>

        <label style="font-size:20px;">Username </label>
        <input type="email" name="email" placeholder="Enter Email as Username" required><br><br>

        <label style="font-size:20px;">Password</label>
        <input type="password" name="password" placeholder="Enter password" required
pattern="^(?=.*[A-Z])(?=.*[a-z])(?=.*[0-9])(?=.*[@#$%^&+=]).{6,}$"
title="Password must contain at least 1 uppercase, 1 lowercase, 1 number, 1 special symbol and minimum 6 characters"><br><br>

        <label style="font-size:20px;">Confirm Password</label>
        <input type="password" name="confirmPassword" placeholder="Enter same password for confirmation" required><br><br>

        <!-- ✅ CENTER BUTTON -->
        <div class="btn-center">
            <button type="submit">Register</button>
        </div>

    </form>

</div>

</body>
</html>






.................result.jsp




<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<!DOCTYPE html>
<html>
<head>
    <title>Winners</title>
   <link rel="stylesheet" href="css/style.css?v=2">
</head>

<body>

<div class="container">

<h1 style="display:flex; align-items:center; justify-content:center; gap:10px;">
<img src="images/trophy.png" style="width:70px; filter: drop-shadow(0 0 10px gold);">
And the Winners are......</h1>


<br><br>

<div class="card">

<%
try {
    Connection con = DBConnection.getConnection();

    PreparedStatement ps = con.prepareStatement(
        "SELECT c.name, n.nominee_name, n.id " +
        "FROM nominees n " +
        "JOIN categories c ON n.category_id = c.id " +
        "WHERE n.vote_count = (" +
        "SELECT MAX(n2.vote_count) FROM nominees n2 WHERE n2.category_id = n.category_id)"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {

        String category = rs.getString("name");
        String winner = rs.getString("nominee_name");
        int id = rs.getInt("id");

        String image = "";

        // 🎬 MAP IMAGES (based on nominee id)
        if(id == 1) image="images/3idiots.jpg";
        else if(id == 2) image="images/drishyam.jpg";
        else if(id == 3) image="images/omshantiom.jpg";
        else if(id == 4) image="images/dhurandhar.jpg";
        else if(id == 5) image="images/taare.jpg";

        else if(id == 6) image="images/srk.jpg";
        else if(id == 7) image="images/hrithik.jpg";
        else if(id == 8) image="images/salman.jpg";
        else if(id == 9) image="images/ajay.jpg";
        else if(id == 10) image="images/anil.jpg";

        else if(id == 11) image="images/alia.jpg";
        else if(id == 12) image="images/rani.jpg";
        else if(id == 13) image="images/aish.jpg";
        else if(id == 14) image="images/kareena.jpg";
        else if(id == 15) image="images/madhuri.jpg";

        else if(id == 16) image="images/amrish.jpg";
        else if(id == 17) image="images/priyanka.jpg";
        else if(id == 18) image="images/prem.jpg";
        else if(id == 19) image="images/aruna.jpg";
        else if(id == 20) image="images/anupam.jpg";
%>

<!-- 🏆 WINNER CARD -->
<div class="category-box winner-card">

    <h2 class="winner-title"><%= category %></h2>
    
    <br><br>

    <div class="image-box">
        <img src="<%= image %>" class="winner-img">
    </div>
    
    <br>

    <h3 class="winner-name">
         <%= winner %>
    </h3>

</div>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</div>

<br><br>

<div class="btn-center">
    <a href="home.jsp">
        <button>Back to Home</button>
    </a>
</div>

</div>

</body>
</html>







...................viewVotes.jsp


<div class="container">
<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<!DOCTYPE html>
<html>
<head>
    <title>Vote Count</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>

<h2 style="color: cyan;">All Vote Counts</h2>

<table border="1">
<tr>
    <th>Category</th>
    <th>Nominee</th>
    <th>Votes</th>
</tr>

<%
try {
    Connection con = DBConnection.getConnection();

    PreparedStatement ps = con.prepareStatement(
        "SELECT c.name, n.nominee_name, n.vote_count " +
        "FROM nominees n JOIN categories c ON n.category_id = c.id " +
        "ORDER BY c.id, n.vote_count DESC"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<tr>
    <td><%= rs.getString("name") %></td>
    <td><%= rs.getString("nominee_name") %></td>
    <td><%= rs.getInt("vote_count") %></td>
</tr>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</table>

<br>
<a href="home.jsp">Back to Home</a>

</body>
</html>
</div>






............villain.jsp



<%@ page import="java.sql.*" %>
<%@ page import="dao.DBConnection" %>

<%@ page isErrorPage="true" %>
<%
    if (session.getAttribute("userId") == null) {
        response.sendRedirect("login.jsp");
        return;
    }
%>

<!DOCTYPE html>
<html>
<head>
    <title>Vote Now</title>
    <link rel="stylesheet" href="css/style.css">
</head>

<body>

<div class="container">

<h2 style="color:#00f5d4; font-size:40px; text-shadow:0 0 10px #00f5d4;">
    Choose Your Bollywood Favorites
</h2><br>

<form action="FinalVoteServlet" method="post">

<div class="category-box">

<h3 style="font-size:28px; color:#ffbe0b; text-shadow:0 0 10px #ffbe0b;">
    Step 4/4 - Best Villain
</h3><br><br>

<hr style="border:none; height:2px; background:linear-gradient(to right, transparent, white, transparent); margin:10px 0 20px;">

<%
try {
    Connection con = DBConnection.getConnection();

    // 🔥 category_id = 4 → Villain
    PreparedStatement ps = con.prepareStatement(
        "SELECT * FROM nominees WHERE category_id = 4"
    );

    ResultSet rs = ps.executeQuery();

    while(rs.next()) {
%>

<label class="vote-option" style="font-size:20px;">
    <input type="radio" name="villain" value="<%= rs.getInt("id") %>" required>

    <img src="<%= rs.getString("image_path") %>" 
         style="width:140px; height:120px;">

    <%= rs.getString("nominee_name") %>
</label>

<%
    }
} catch(Exception e) {
    e.printStackTrace();
}
%>

</div>

<br><br>

<div class="btn-center">
    <button type="submit">Submit vote</button>
</div>

</form>

</div>

</body>
</html>






awards.jsp





package controller;

import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.*;
import java.io.IOException;
import java.sql.*;

import dao.DBConnection;

@WebServlet("/AddNomineeServlet")
public class AddNomineeServlet extends HttpServlet {

    protected void doPost(HttpServletRequest req, HttpServletResponse res)
            throws ServletException, IOException {

        int categoryId = Integer.parseInt(req.getParameter("categoryId"));
        String nominee = req.getParameter("nominee");
        String image = req.getParameter("image");

        try {
            Connection con = DBConnection.getConnection();

            PreparedStatement ps = con.prepareStatement(
                "INSERT INTO nominees(category_id, nominee_name, image_path) VALUES (?, ?, ?)"
            );

            ps.setInt(1, categoryId);
            ps.setString(2, nominee);
            ps.setString(3, image);

            ps.executeUpdate();

            // ✅ Redirect properly
            res.sendRedirect(req.getContextPath() + "/adminDashboard.jsp?msg=Nominee Added");

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}






















